# local  = data v localStorage prohlizece (funguje bez backendu)
# http   = data z PHP API (viz docs/api-contract.md)
VITE_API_MODE=local
VITE_API_URL=/api

# Pouziva se pouze v local modu. V http modu heslo overuje backend.
VITE_EDIT_PASSWORD=trener
